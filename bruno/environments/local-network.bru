// Device connected to your LAN: replace the IP with the one shown on the
// diagnostics screen (or GET /status while on the same network).
vars {
  baseUrl: http://192.168.1.123
  ssid: MyNetwork
  password: change-me
  username: Test Boat
  timezoneOffset: 1
}
